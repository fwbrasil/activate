	/*** RESIZE TO 100% HEIGHT SIDEBAR ***/		var sidenav, content;		/*** function that verify the heights and set the sidenav height ***/	function resizeSidenav() {			sidenav = $("#side-nav").height();				content = $("#main-content").height();				if(sidenav < content && $(document).width() > 767) {			$("#side-nav").height(content);		}	}		$(window).load(function() {		$('.flexslider').flexslider( { start: function(){ 						resizeSidenav();		} } );	});			$(document).bind("loaded", function() { resizeSidenav(); });				$(document).ready(function() {						resizeSidenav();				$(window).resize(function() {			if($(document).width() < 767) 				$("#side-nav").height("auto");			else 				resizeSidenav();		});			});	/*** END # RESIZE TO 100% HEIGHT SIDEBAR ***/		$(document).ready(function($){		/** NAVIGATION TOGGLE **/				$("#header-wrapper-top #navigation-toggle a").click(function() {			$(this).parents("#header-wrapper-top").children("#top-menu").slideToggle("slow");			return false;		});							/** SEARCH INIT **/			var searchstr = 'Enter keyword and hit enter';			var search = $("#searchform #s");		if(search.length > 0) {			if(search.val() == '') {				search.val(searchstr);			}						search.focus( function() {				if(search.val() == searchstr) {					search.val('');				}			});						search.blur( function() {				if(search.val() == '') {					search.val(searchstr);				}			});		}		/** ISOTOPE FILTER **/			var $container = $('#portfolio-wrapper');				$container.isotope({ filter: '.all' });				$container.isotope();				if($container.height() == 0) {			$container.isotope( 'destroy' );			$container.isotope( 'reloadItems' );			$container.height("auto");		}						$(window).resize(function() {			$container.isotope('reLayout');		});				$('#portfolio-filter-wrapper a').click(function(){					$('#portfolio-filter-wrapper a').removeClass("active");			var selector = $(this).attr('data-filter');			$container.isotope({ filter: "." + selector });						$(this).addClass("active");						return false;		});			/** PORTFOLIO GRID ITEM HOVER **/				$('.project-grid').hover( function() {			$(this).children(".overlay").fadeIn('fast');		}, function() {			$(this).children(".overlay").fadeOut('fast');		});			/** BACK TO TOP **/				$(".back-top a").click(function(event) {          $('html,body').animate({			scrollTop: 0			}, 500);		  return false;        });				});		/** ORDERNOW INPUT **/	var d = document;	var safari = (navigator.userAgent.toLowerCase().indexOf('safari') != -1) ? true : false;	var gebtn = function(parEl,child) { return parEl.getElementsByTagName(child); };	onload = function() {		var body = gebtn(d,'body')[0];		body.className = body.className && body.className != '' ? body.className + ' has-js' : 'has-js';		if (!d.getElementById || !d.createTextNode) return;		var ls = gebtn(d,'label');		for (var i = 0; i < ls.length; i++) {			var l = ls[i];			if (l.className.indexOf('label_') == -1) continue;			var inp = gebtn(l,'input')[0];			if (l.className == 'label_check') {				l.className = (safari && inp.checked == true || inp.checked) ? 'label_check c_on' : 'label_check c_off';				l.onclick = check_it;			};			if (l.className == 'label_radio') {				l.className = (safari && inp.checked == true || inp.checked) ? 'label_radio r_on' : 'label_radio r_off';				l.onclick = turn_radio;			};		};	};	var check_it = function() {		var inp = gebtn(this,'input')[0];		if (this.className == 'label_check c_off' || (!safari && inp.checked)) {			this.className = 'label_check c_on';			if (safari) inp.click();		} else {			this.className = 'label_check c_off';			if (safari) inp.click();		};	};	var turn_radio = function() {		var inp = gebtn(this,'input')[0];		if (this.className == 'label_radio r_off' || inp.checked) {			var ls = gebtn(this.parentNode.parentNode,'label');			for (var i = 0; i < ls.length; i++) {				var l = ls[i];				if (l.className.indexOf('label_radio') == -1)  continue;				l.className = 'label_radio r_off';			};			this.className = 'label_radio r_on';			if (safari) inp.click();		} else {			this.className = 'label_radio r_off';			if (safari) inp.click();		};	};